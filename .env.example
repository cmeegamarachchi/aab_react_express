# Environment Variables for AAB React Express Application

# Server Configuration
NODE_ENV=production
PORT=8324

# Frontend Distribution Path (for traditional deployment)
# In Docker, this is automatically set to 'frontend-dist'
FRONTEND_DIST=dist/frontend

# CORS Configuration
CORS_ORIGIN=*

# Database Configuration (if you add database support)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# API Keys and Secrets (add as needed)
# JWT_SECRET=your-jwt-secret-here
# API_KEY=your-api-key-here
